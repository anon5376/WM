# WM001 Bootstrap Notes

Night 2R starts fresh because no WM0 checkout exists locally.

`readforcodex.md` is kept at the repository root as the operator-facing constitution pointer. `docs/WM_PROGRAM.md` is the byte-identical committed constitution copy used by repo tooling and reports.

No `WM0_SPEC.md` was present during bootstrap, so no `docs/WM0_SPEC_HISTORICAL.md` file was created.
